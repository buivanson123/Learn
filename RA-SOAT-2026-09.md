# Rà soát toàn bộ giáo án + bộ câu hỏi (12/09/2026)

Thước đo dùng để chấm: **đi phỏng vấn Middle/Senior**. Hai trục soi: *quá bleeding-edge* và
*câu hỏi không giống phỏng vấn thật*.

Mọi nhận xét dưới đây kèm **vị trí file** và **output đo được**, không có câu nào nói suông.

---

## Kết luận ngắn

| Phần | Mức độ lệch thực tế | Việc cần làm |
|---|---|---|
| `css/` | **Lệch nặng** | Hạ tính năng Chrome-only xuống mục "sắp tới", thêm Tailwind/SCSS/fallback |
| `laravel/` | **Lệch nặng** | Chỉ dạy Laravel 13; thiếu cầu nối sang codebase 10/11/12 mà công ty đang chạy |
| `nextjs/` | Lệch vừa | Next 16-only; Pages Router có nhắc nhưng quá mỏng |
| `vuejs/` | Lệch nhẹ | Tên file neo phiên bản (`06-pinia-4.md`); thiếu đối chiếu Pinia 2/router 4 |
| `typescript/` | Lệch nhẹ | TS 7 là đúng hướng, nhưng phải nói rõ dự án thật còn 5.x |
| `nestjs/` | **Ổn** | Đã xử lý phiên bản tử tế (xem C10); chỉ cần bỏ câu trivia phiên bản |
| `javascript/` | **Ổn** | Bộ bài tập đúng chuẩn live-coding; chỉ sửa cách đặt đề vài câu |
| `redis/`, `docker/`, `linux/` | **Tốt** | Câu hỏi sát thực tế; `redis/` hơi nặng Sentinel/Cluster cho app dev |
| `phongvan/` (senior) | **Tốt nhất repo** | Đây là khuôn mẫu nên nhân ra các bộ khác; chỉ thiếu nhánh Vue/Next |

Một điều **không** phải vấn đề, đã kiểm tra để loại trừ: độ dài "Trả lời ngắn" là nói được ra miệng.

```
css/phong-van/01-cau-hoi-va-dap-an.md           n=61  trung vị= 21 từ  max=42 từ
javascript/phong-van/01-cau-hoi-va-dap-an.md    n=54  trung vị= 20 từ  max=37 từ
```

---

## 1. Toàn bộ repo neo vào **bản vừa phát hành**, không phải bản thị trường đang dùng

Đây là gốc của cảm giác "không thực tế". Kiểm tra phiên bản ghi trong tài liệu so với `npm view`
hôm nay — chúng **trùng khớp tuyệt đối**, nghĩa là giáo án được viết ở mép trước của hệ sinh thái:

```
$ npm view <pkg> version        (12/09/2026)
next           16.3.5      ← tài liệu: "Next 16", 39 lần / 10 file
typescript     7.0.2       ← tài liệu: "TypeScript 7", 10 lần / 6 file
@nestjs/core   12.0.1
typeorm        1.1.1       ← nhánh 0.3.x đã mang tag legacy
vue            3.5.42
vue-router     5.3.1       ← tài liệu: 05-vue-router-5.md
pinia          4.0.3       ← tài liệu: 06-pinia-4.md
vite           8.3.0
tailwindcss    4.3.3
```

Vấn đề không phải "dạy bản mới là sai". Vấn đề là **không có cầu nối về bản cũ**, trong khi codebase
công ty và người phỏng vấn đang ở N-1/N-2:

```
$ grep -rli "Laravel 11" --include='*.md' .   ->  0 file
$ grep -rli "Laravel 12" --include='*.md' .   ->  0 file
$ grep -rli "Livewire 3" --include='*.md' .   ->  0 file
$ grep -rli "Pinia 2"    --include='*.md' .   ->  0 file
$ grep -rli "Vite 6"     --include='*.md' .   ->  0 file
$ grep -rli "vue-router 4" --include='*.md' . ->  1 file
```

### Ví dụ cụ thể nhất — `laravel/phong-van/01-cau-hoi-va-dap-an.md:323`

```
### B6 Middleware là gì? Đăng ký ở đâu trong Laravel 13?
**Ngắn:** ... Laravel 13 khai trong `bootstrap/app.php` ở `->withMiddleware()`,
          không còn `app/Http/Kernel.php`.
```

Và `:343`:

```
### B7 ⭐ Vì sao `$this->middleware('auth')` trong constructor không chạy nữa?
```

Cả hai câu đúng với Laravel 13 và **vô dụng nếu công ty đang chạy Laravel 10**. Người phỏng vấn sẽ
hỏi ngược: "ở dự án bọn em middleware group khai trong `Kernel.php`, em sửa ở đâu?" — đó là câu mà
giáo án hiện tại không chuẩn bị, vì `Kernel.php` chỉ xuất hiện như thứ "không còn nữa".

**Sửa:** mỗi câu neo phiên bản thêm một dòng `Trên bản cũ (10/11):` + đoạn code tương đương. Không cần
viết lại bài, chỉ thêm 2–4 dòng mỗi câu.

### Chỗ đã làm đúng — `nestjs/phong-van/01-cau-hoi-va-dap-an.md:764`

```
### C10 ⭐ TypeORM đang ở phiên bản nào?
**Ngắn:** `latest` hiện là 1.1.0; nhánh 0.3.x đã mang tag `legacy`.
         `@nestjs/typeorm@11.0.3` khai hỗ trợ cả hai (`typeorm: '^0.3.0 || ^1.0.0-dev'`)
```

Đây là cách xử lý phiên bản đúng: nói bản mới **và** bản dự án thật đang dùng. Nhân mẫu này ra
Laravel / Next / Vue.

---

## 2. `css/` dạy tính năng **chưa chạy được đa trình duyệt** như thể đó là cách làm mặc định

Đây là lỗi nặng nhất của repo, vì nó dẫn tới code không ship được.

Đo bằng dữ liệu compat của MDN (`@mdn/browser-compat-data` 8.1.1, cập nhật 10/09/2026 — 2 ngày trước):

```
tính năng             Chrome  Safari  Firefox  Safari iOS
interpolate-size      129     KHÔNG   KHÔNG    KHÔNG        ← Chrome-only
animation-timeline    115     26      preview  26           ← chưa vào Firefox bản ổn định
scroll-timeline       115     26      preview  26
@scope                118     26.4    146      26.4         ← mới ra, máy khách còn lâu mới có
field-sizing          123     26.2    152      26.2
anchor-name           125     26      147      26
-- các thứ đã an toàn, dạy thẳng được --
@layer                99      15.4    97       15.4
:has()                105     15.4    121      15.4
@container            105     16      110      16
@starting-style       117     17.5    129      17.5
transition-behavior   117     17.4    129      17.4
light-dark()          123     17.5    120      17.5
oklch()               111     15.4    113      15.4
subgrid               117     16      71       16
```

### Khẳng định sai lệch — `css/09-transition-va-animation.md:7` và `:12`

```
| Mở accordion mượt | `max-height: 1000px` (số ma thuật) | `interpolate-size: allow-keywords` |
...
Cả bốn đều đã chạy trên Chrome 152. Bài này đo từng cái.
```

`css/README.md` lặp lại y hệt trong bảng "Cách cũ vẫn được dạy / Cách bây giờ", và
`css/phong-van/01-cau-hoi-va-dap-an.md:1444` hỏi "Làm sao animate `height: auto`?".

Thực tế: accordion viết theo cách này **đứng yên trên Safari và Firefox** — tức là trên iPhone của
người dùng. `max-height` không phải "cách cũ", nó vẫn là cách duy nhất chạy mọi nơi. Nếu trả lời
phỏng vấn rằng "`max-height` là số ma thuật, giờ dùng `interpolate-size`", người phỏng vấn có kinh
nghiệm sẽ hỏi lại "Safari thì sao?" và đó là câu trượt.

### Thiếu hẳn kỷ luật fallback

```
$ grep -rn "@supports" css/          ->  chỉ 2 dòng, đều trong css/00-moi-truong-va-devtools.md
$ grep -rli "polyfill" css/          ->  0 file
$ grep -rli "autoprefixer\|postcss" css/ -> 0 file
```

60 câu phỏng vấn CSS **không có câu nào** hỏi "tính năng này chưa hỗ trợ thì bạn làm gì" — trong khi
đó là câu hỏi FE thật gặp hằng ngày.

**Sửa:**
1. `css/README.md` + `css/09`: đổi cột "Cách bây giờ" → hai cột *"Ship được hôm nay"* và
   *"Chrome-only, dùng làm progressive enhancement"*. `interpolate-size`, `animation-timeline`,
   `@scope`, `field-sizing`, `anchor-name` chuyển sang cột 2.
2. Thêm 1 bài hoặc 1 mục về `@supports` + mẫu progressive enhancement, dùng đúng bảng compat trên.
3. Thêm 3–4 câu phỏng vấn: "`:has()` dùng được chưa, kiểm tra thế nào?", "animate `height:auto`
   cho cả Safari?", "fallback cho `light-dark()`?".

---

## 3. `css/` thiếu đúng những thứ phỏng vấn FE **luôn** hỏi

Quét 60 câu + 18 bài tập + 10 tình huống:

```
$ grep -ci "tailwind" css/phong-van/01-cau-hoi-va-dap-an.md   -> 0
$ grep -ci "bem"      css/phong-van/01-cau-hoi-va-dap-an.md   -> 0
$ grep -ci "scss"     css/phong-van/01-cau-hoi-va-dap-an.md   -> 0
$ grep -ci "srcset\|<picture" css/                            -> 0 file
```

`sass` chỉ xuất hiện làm điểm đối chiếu (câu 6, câu 47). `tailwind` trong cả thư mục `css/` xuất hiện
**đúng 1 lần**, ở `css/phong-van/README.md` — và là câu để *ứng viên hỏi ngược lại* nhà tuyển dụng.

Thực tế tuyển Middle/Senior FE hiện nay: JD gần như luôn có Tailwind hoặc SCSS, và vòng kỹ thuật hay
hỏi "vì sao Tailwind thắng/thua CSS thuần", "utility-first đánh đổi gì", "quản lý design token",
"@apply có nên dùng". Bộ hiện tại không có câu nào.

**Sửa:** thêm mục G — *CSS trong dự án thật* (8–10 câu): Tailwind vs CSS thuần vs CSS Modules, design
token, quy ước đặt tên (BEM và vì sao `@layer` làm nó bớt cần thiết), ảnh responsive (`srcset`,
`<picture>`, `loading=lazy`), cross-browser, review CSS của người khác.

---

## 4. Vài câu không ai hỏi thật — đúng nhưng không phải câu phỏng vấn

Phần lớn câu trong repo là câu hỏi thật. Nhưng có một nhóm nhỏ bị "đề hóa" từ chính số đo của máy:

| Vị trí | Câu | Vấn đề |
|---|---|---|
| `nestjs/.../01:764` | "TypeORM đang ở phiên bản nào?" | Đáp án tự thừa nhận "điểm cộng nhỏ". Không ai hỏi số phiên bản. Nên đổi thành "Nâng TypeORM 0.3 → 1.x, bạn lo gì nhất?" |
| `javascript/.../01:143` | "Vì sao giữ một hàm trả về số `1` lại có thể giữ 152 MB?" | 152 MB là số của máy bạn. Đổi thành "Closure giữ gì trong scope? Làm sao biết?" rồi để 152 MB ở phần đào sâu |
| `javascript/.../01:1402` | "`Proxy` đắt bao nhiêu?" | Đổi: "Khi nào bạn không dùng Proxy?" |
| `javascript/.../01:1561` | "`Map` và `WeakMap` khác nhau ở đâu? Đo được bao nhiêu?" | Bỏ nửa sau khỏi đề |
| `javascript/.../01:1900` | "Layout thrashing là gì? Đo được bao nhiêu?" | Bỏ nửa sau khỏi đề |
| `css/.../01:548` | "Thanh cuộn chiếm bao nhiêu chỗ?" | Hỏi thật sẽ là "`100vw` gây cuộn ngang, vì sao?" |

Quy tắc chung: **số đo thuộc phần đáp án, không thuộc phần đề.** Đề phải là câu một người phỏng vấn
nói ra được mà không cần đọc tài liệu này.

---

## 5. Bộ senior FE chỉ có nhánh React, trong khi bạn học Vue + Next

`phongvan/frontend/03-react-va-quan-ly-state.md` — 16 câu, toàn React/Redux/Zustand/TanStack:

```
$ grep -rhoiE "React [0-9]+|Vue [0-9]|Redux|Zustand|TanStack" phongvan/frontend/ | sort | uniq -c
   5 Redux     4 Zustand     3 TanStack     2 React 19     1 React 18
   (Vue: 0 lần)
```

Bạn có cả `vuejs/` và `nextjs/` nhưng vòng senior FE lại chỉ luyện React. Nếu nhắm JD Vue thì thiếu
hẳn các câu mức senior: reactivity caveat khi scale, `shallowRef` cho dữ liệu lớn, tổ chức store khi
có 40 store, SSR hydration mismatch trong Nuxt/Vue.

**Sửa:** thêm `phongvan/frontend/03b-vue-va-quan-ly-state.md` đối xứng, hoặc đổi 03 thành
"quản lý state" trung lập rồi chia hai nhánh trong từng câu.

---

## 6. Lộ trình ghi trong README không khả thi

Cộng cột "Thời lượng" trong từng README:

```
css          Lộ trình 7 ngày   tổng= 41h     (5.9h/ngày)
javascript   Lộ trình 8 ngày   tổng= 55h     (6.9h/ngày)
typescript   Lộ trình 5 ngày   tổng= 31h     (6.2h/ngày)
vuejs        Lộ trình 7 ngày   tổng= 32h
nextjs       Lộ trình 7 ngày   tổng= 36h
nestjs       Lộ trình 7 ngày   tổng= 42h
laravel      Lộ trình 7 ngày   tổng= 47h
redis        Lộ trình 7 ngày   tổng= 39h
docker       Lộ trình 3 ngày   tổng= 15h
                               ─────────
                               338h + linux + phongvan
```

338 giờ chia cho "7 ngày mỗi môn" = 6–7h học **mỗi ngày, liên tục**, tức chỉ đúng nếu bạn nghỉ làm.
Với người đi làm học buổi tối (1.5–2h/ngày), `css/` là **3 tuần** chứ không phải 7 ngày.

**Sửa:** đổi nhãn "Lộ trình 7 ngày" → "≈41 giờ học — 3 tuần nếu học 2h/tối", và thêm **lộ trình rút
gọn trước phỏng vấn**: chỉ những bài có câu ⭐, ví dụ CSS 8h thay vì 41h.

---

## 7. Không nhất quán giữa các bộ (dễ sửa, nên sửa sớm)

Nhãn đáp án mỗi bộ một kiểu:

```
css, javascript        **Trả lời ngắn:** / **Giải thích sâu:**
laravel                lẫn cả hai kiểu trong cùng một file
docker, nestjs, nextjs, redis, typescript, vuejs, linux   **Ngắn:** / **Đào sâu:**
```

Dấu ⭐ (mức ưu tiên ôn) có ở 7 bộ nhưng **không có** ở 3 bộ quan trọng nhất:

```
css/phong-van/01          0⭐ / 60 câu
javascript/phong-van/01   0⭐ / 72 câu
phongvan/** (senior)      0⭐ / 184 câu
```

Hệ quả thực tế: không có cách nào ôn gấp. `redis` có 43/70 câu ⭐, `laravel` 34/80 — dùng được ngay;
còn CSS và JS thì phải đọc cả 132 câu.

---

## Việc cần làm, theo thứ tự

**Ưu tiên 1 — sai lệch có thể làm trượt phỏng vấn**
1. `css/README.md`, `css/09-transition-va-animation.md:7-12`, `css/phong-van/01:1444,1510`: tách
   "ship được" khỏi "Chrome-only", dán bảng compat thật vào.
2. `laravel/phong-van/01` + `laravel/0*`: thêm dòng "trên bản 10/11" cho mọi câu neo Laravel 13
   (ít nhất B6, B7, C4, và các câu dùng `bootstrap/app.php`).
3. `nextjs/`: nâng phần Pages Router / `getServerSideProps` lên thành mục riêng trong bộ câu hỏi.

**Ưu tiên 2 — thiếu nội dung phỏng vấn thật hỏi**
4. Thêm mục "CSS trong dự án thật" (Tailwind/SCSS/token/ảnh responsive/cross-browser) — 8–10 câu.
5. Thêm nhánh Vue cho `phongvan/frontend/03`.
6. Thêm 3–4 câu fallback/`@supports` vào bộ CSS.

**Ưu tiên 3 — dọn dẹp**
7. Sửa 6 câu "đề hóa từ số đo" ở mục 4.
8. Thống nhất nhãn `**Ngắn:**` / `**Đào sâu:**` cho cả repo (đang là mẫu của 7/10 bộ).
9. Gắn ⭐ cho `css/`, `javascript/`, `phongvan/` + viết lộ trình rút gọn theo ⭐.
10. Sửa nhãn thời lượng trong 9 README.

---

## Những thứ tốt — đừng sửa

- `phongvan/README.md`: bảng "Middle trả lời / Senior trả lời" + công thức 3 tầng + ví dụ
  `SELECT *` kèm `Heap Fetches: 0, 0.551 ms`. Đây là chất lượng cao nhất repo.
- `css/phong-van/03-tinh-huong-debug.md`: 10 tình huống viết đúng bằng lời người dùng thật
  ("Em để z-index 9999 rồi", "Người dùng báo trang khó dùng bằng bàn phím").
- `javascript/phong-van/02-bai-tap-thuc-hanh.md`: 22 bài trùng đúng đề live-coding hay gặp —
  debounce, throttle, curry, memoize, pLimit, deepClone, EventEmitter, LRU, flatten.
- `docker/phong-van/01`: 40 câu gần như không có câu nào lệch thực tế (`exec format error`,
  `depends_on` không đợi, server đầy đĩa).
- Độ dài "Trả lời ngắn" trung vị 20–21 từ — đúng cỡ nói ra miệng.
